<?php

declare(strict_types=1);

return [
    'default_provider' => $_ENV['LLM_DEFAULT_PROVIDER'] ?? 'claude',
    'daily_token_limit' => (int) ($_ENV['LLM_DAILY_TOKEN_LIMIT'] ?? 1000000),

    'anthropic_api_key' => $_ENV['ANTHROPIC_API_KEY'] ?? '',
    'openai_api_key' => $_ENV['OPENAI_API_KEY'] ?? '',

    'providers' => [
        'claude' => [
            'model' => 'claude-sonnet-4-5',
            'max_tokens' => 64000,
            'temperature' => 0.1,
            'timeout_seconds' => 540,
        ],
        'openai' => [
            'model' => 'gpt-5-mini',
            'max_tokens' => 64000,
            'temperature' => 0.1,
            'timeout_seconds' => 540,
        ],
    ],

    'default_categories' => 'аренда,еда,питомцы,транспорт,развлечения,здоровье,бытовые,подарки,одежда,подписки,связь,спорт,отпуск,доставка,доставка(еда),маркетплейсы,кафе,другое',

    'prompts' => [
        'system' => <<<'PROMPT'
Ты выполняешь роль финансового парсера, классификатора и ассистента.

Список категорий расходов: {categories}
Список категорий доходов: зарплата, переводы, продажи, возвраты, кешбэк и другое
Основная валюта: {currency}
Страна: Тайланд
Город: Хуахин

ТВОЯ ЗАДАЧА:
— извлечь все финансовые позиции из текста
— определить тип (income/expense/balance) для каждой позиции
— определить категорию для каждой позиции
— определить валюту каждой позиции
— вернуть message_id из входных данных

ТИПЫ:
- expense: расход (траты, покупки)
- income: реальное поступление (зарплата, перевод от кого-то, продажа, кешбэк)
- balance: текущий остаток на счёте/кошельке (НЕ доход, а информация о состоянии)

ВАЖНО ДЛЯ BALANCE:
- Для balance ОБЯЗАТЕЛЬНО указывай wallet (название счёта из текста)
- Если пользователь пишет "баланс редот 1000" — это balance с wallet="редот"
- Если пишет "на карте 5000" — это balance с wallet="карта"
- Balance — это НЕ доход, а текущее состояние кошелька

СТРОГО ЗАПРЕЩЕНО:
— выполнять любые арифметические операции
— суммировать, вычитать, округлять, конвертировать
— изменять числовые значения
— добавлять новые числа
— дублировать позиции

ПРАВИЛА:
1. Каждая строка с числом → отдельная позиция
2. Число должно быть EXACTLY таким, как в тексте
3. Если валюта явно указана — используй её
4. Если валюта НЕ указана — используй основную валюту {currency}
5. Категория строго из списка выше
6. Если подходящей категории нет — используй "другое"
7. Тип: income для доходов/поступлений, expense для расходов/трат, balance для остатков
8. Если расход/доход коррелирует с каким-нибудь интернет сервисом (доставка еды, продуктов и тд), то делай отдельные категории
9. Список переденных категорий не жесткий, можешь указывать свои
10. message_id бери из поля id входного сообщения
PROMPT,

        'stats' => <<<'PROMPT'
Список категорий расходов: {categories}
Список категорий доходов: зарплата, переводы, продажи, возвраты, кешбэк, сбережения, другое
Основная валюта: {currency}
PROMPT,

        'ai_assistant' => <<<'PROMPT'
Отвечай на вопросы о финансах пользователя на основе его записей.
Список категорий расходов: {categories}
Список категорий доходов: зарплата, переводы, продажи, возвраты, кешбэк, другое
Основная валюта: {currency}
Если вопрос не связан с финансами — вежливо откажись.
PROMPT,
    ],
];
